# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type UserType {
  id: ID!
  email: String
  nickname: String
  profileImage: String
  role: String
}

type MessageType {
  id: ID!
  message: String!
  participant: UserType
  createdAt: DateTime
  roomId: Int
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type RoomInfoType {
  roomId: Int!
  recipientId: Int!
}

type AdminRoomType {
  roomId: Int!
  participantIds: [Int!]!
  created: DateTime!
}

type PaginatedAdminRooms {
  data: [AdminRoomType!]!
  total: Int!
  page: Int!
  take: Int!
}

type AiPersonalityInfoType {
  personality: AiPersonality
  canChange: Boolean!
}

enum AiPersonality {
  FRIENDLY
  CODING
  ENGLISH
  CREATIVE
}

type Query {
  getAllRooms(page: Int = 1, take: Int = 20, sort: String = "DESC", sortBy: String = "id"): PaginatedAdminRooms!
  ping: String!
  getAiUserId: Int!
  getAiPersonalityInfo(roomId: Int!): AiPersonalityInfoType
  getOnlineUser: [Int!]!
  getAllUsers: [Int!]!
  getUserNicknames: [UserType!]!
  getMyRooms: [RoomInfoType!]!
  getRoom(recipientId: Int!): Int
  getMessages(roomId: Int!, cursor: Int): [MessageType!]!
}

type Mutation {
  deleteRoom(roomId: Int!): Boolean!
  setAiPersonality(roomId: Int!, personality: AiPersonality!): Boolean!
  sendMessage(input: CreateChatInput!, recipientId: Int!): MessageType!
}

input CreateChatInput {
  message: String!
  aiPersonality: AiPersonality
}

type Subscription {
  receiveMessage(roomId: ID!): MessageType!
}