[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "sh -c 'cd backend && pnpm migration:run && node dist/main'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3